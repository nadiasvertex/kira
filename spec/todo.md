1. Full Unicode case mapping and folding for `std.string` are done (`std.unicode`/`std.unicode_tables`, pure Kira over generated UCD tables — see [52-std-string.md](specification/04-stdlib/strings-and-formatting/52-std-string.md)). Still deferred, each needing more work than "read another generated table": locale-tailored casing (Turkish/Azeri dotted/dotless-I, Lithuanian dot-retention — needs a locale parameter threaded through every casing call), NFC/NFD normalization, and grapheme-cluster segmentation (UAX #29) so `reversed`/`split` can operate on graphemes rather than scalars.
2. "with" style scoped blocks.
3. store and load binary AST instead of reparsing
4. store and load generated bytecode instead of recompiling
5. A derivation that dispatches on a field's *concrete* type. `f.type_name` (`render_type_expr`, `src/comptime/reflect.cpp`) is still syntactic — generic arguments collapse to `"[..]"`, every non-`named_type` shape renders `"<type>"`, aliases are unresolved, and inside a generic declaration a `T` field reports `"T"` rather than the instantiation's type, because `comptime::value`'s type value carries no bindings. None of the five derivations reads `type_name`, so nothing is blocked; the working mechanism for type-dependent behaviour today is `T.name()` in ordinary generic code (`type_param_reflection`), which is how `std.algo`'s `sort[T]` selects a radix sort.
6. `zero`/`one`/`hash` have no impls for the 128-bit types (`int128`, `uint128`, `float128`), because a reachable module is compiled in full and the bytecode VM has no representation for them yet — an `impl zero for int128` in `std.traits` makes every program fail to compile. They arrive with codegen increment 6, alongside the rest of 128-bit support.
7. `hash` has no impls for `float32`/`float64`, so `type p = { x: float64 } deriving hash` type-checks and then reports "no method `hash` on `float64`". Hashing a float correctly means hashing its *bit pattern*, and `as uint64` on a float is a value conversion, not a reinterpret (`compile_cast`, `src/llvm_codegen/codegen.cpp`, emits `FPToUI`) — so this needs a reinterpret/bitcast primitive, which is the rare intrinsic that passes clause (a) of the justification test: it is genuinely inexpressible over the primitive set. Two semantic decisions have to be made explicitly rather than inherited from the bit pattern: `0.0` and `-0.0` compare equal but have different bits, and every NaN compares unequal to itself while there are 2^52 distinct NaN bit patterns — a `hash` that disagrees with `eq` on either is a broken hash.
8. An `impl` block parameterized by a *value* — `impl[n: usize] show for buf[n]` — reports "cannot tell which `n` this call to `show` means". `check_impl_generic_method_call` (`src/semantic/check.cpp`) and `check_impl_generic_static_call` iterate every entry of `block_type_params` and require each to appear in `bindings`, which is a `std::unordered_map<std::string, type_id>` populated by `unify_rigid` and so cannot hold a value binding at all. Needs the `value_bindings` channel `solve_generic_params` already uses for free functions extended to impl blocks, plus an instance suffix that distinguishes `buf[4]` from `buf[8]` (`mangle_type_for_instance` may already separate them — verify rather than assume, since a collision there silently compiles one body for both). The diagnostic is also the wrong shape for this case: it blames the user's call site and suggests moving `n` onto the method, for a parameter the receiver plainly determines.
9. DONE. `!=`/`<=`/`>=` inside a string interpolation now parse correctly. `scan_interpolated_content` (`src/parser/interp_string.cpp`) implements the self-documenting `{expr=}` form by cutting the expression off at the first bare `=`; it detected "bare" as "not immediately followed by another `=`", so the second `=` of `==` and the `=` of `!=`/`<=`/`>=` all qualified, silently truncating the expression (`{a == b}` parsed as `{a=}`-style self-doc for just `a`, discarding `== b` with no diagnostic at all — worse than the reported `!=` parse error, which was at least visible) or, for `!=`, leaving a lone `!` for the sub-lexer to reject. Fixed by requiring the marker `=` to be immediately followed by `}` or `:` (the only two characters that can follow a real self-doc marker per `53-std-format.md`), which both operators never satisfy. Regression test: `parser_test.cpp`'s `==`/`!=`/`<=`/`>=`-in-interpolation block.
