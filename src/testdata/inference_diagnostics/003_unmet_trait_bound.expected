error: `biggest` needs `T: ordering`, and `point` does not satisfy it
  --> 003_unmet_trait_bound.cn:21:5
   21 |     biggest(p, q)
      |     ^^^^^^^^^^^^^ `T` is `point` in this call
  note: `biggest`'s bound on `T` is declared here
    --> 003_unmet_trait_bound.cn:14:42
     14 | def biggest[T](a: T, b: T) -> T where T: ordering:
        |                                          ^^^^^^^^ the bound
  help: Pass a type that implements `ordering`, or implement it for `point` — `impl ordering for point:`.

