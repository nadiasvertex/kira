module stress.stmt_if_let_shared_super

def run(raw_index) -> option[super.value]:
  let shared_cfg: shared config_t = shared load_config("app.toml")
  if let some(i) = index.try_from(raw_index):
    return some(i)
  return none
