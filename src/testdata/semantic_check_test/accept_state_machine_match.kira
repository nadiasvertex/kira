module sample

type closed = { }
type open = { }

type connection[S] = { addr: str }

def send(c: connection[open], data: str) -> unit:
    unit

def run(c: connection[open]) -> unit:
    send(c, "hi")
