module sample
def run() -> unit:
    let numbers = [1, 2, 3, 4, 5]
    let doubled = numbers.filter(x => x > 2)
    var total = 0
    for n in numbers:
        total += n
    let squares = for x in 0..5 => x * x
    while total > 0:
        total -= 1
    println("{total}")
