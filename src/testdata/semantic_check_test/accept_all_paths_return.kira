module accept_all_paths_return

type shade = @light | @dark

def direct() -> int64:
    return 1

def tail_expr() -> int64:
    42

def both_paths(flag: bool) -> int64:
    if flag:
        return 1
    else:
        return 2

def early_then_tail(flag: bool) -> int64:
    if flag:
        return 1
    return 2

def nested(flag: bool, other: bool) -> int64:
    if flag:
        if other:
            return 1
        else:
            return 2
    else:
        return 3

def match_all_arms(s: shade) -> int64:
    match s:
        @light => 1
        @dark =>:
            return 2

def loop_forever(n: int64) -> int64:
    var i: int64 = 0
    while true:
        i = i + 1
        if i > n:
            return i

def unit_fn(flag: bool):
    if flag:
        return
