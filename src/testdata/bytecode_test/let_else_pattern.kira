module sample
type maybe = @present(int32) | @absent
def unwrap_or(v: maybe) -> int32:
    let @present(n) = v else:
        return -1
    return n
def some_case() -> int32:
    return unwrap_or(@present(7))
def none_case() -> int32:
    return unwrap_or(@absent)
