module sample
def sum_of_fives(n: usize) -> int32:
    let xs: list[int32] = [5; n]
    var total = 0
    var i: usize = 0
    while i < n:
        total = total + xs[i]
        i = i + 1
    return total
