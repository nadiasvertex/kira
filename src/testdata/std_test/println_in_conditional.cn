module main

def classify(n: int32) -> unit:
    if n > 0:
        println("positive")
    elif n < 0:
        println("negative")
    else:
        println("zero")

def main() -> int32:
    classify(5)
    classify(-3)
    classify(0)
    return 0
